# Benzina — Progress Log

## Completed
- [x] Session 1: Project scaffold (Next.js, Tailwind, shadcn)
- [x] Session 2: i18n setup (en/ar, RTL, middleware, translation files)
- [x] Session 3: Supabase client setup + types + constants
- [x] Session 4: Header component with language switcher
- [x] Session 5: DGO station scraper (scripts/scrape-dgo-stations.ts)
- [x] Session 6: Full data pipeline (OSM fetch, merge, seed)
  - fetch-osm-stations.ts: 158 stations from Overpass API (Greater Beirut bbox)
  - merge-stations.ts: distance-based matching (100m threshold) of DGO + OSM data
    → 316 merged stations (113 matched pairs, 184 DGO-only, 45 OSM-only)
    → Brand inference via regex (TotalEnergies, Medco, IPT, Coral, Hypco, etc.)
    → Unique slug generation with deduplication
  - seed-db.ts: batch upsert (50/batch) to Supabase with PostGIS POINT geometry
  - Utility modules: geo.ts (haversine), supabase-admin.ts (service role client)
- [x] Session 7: Fuel prices scraper (scripts/scrape-dgo-prices.ts)
  - CLI-based: npx tsx scripts/scrape-dgo-prices.ts --95=... --98=... --diesel=... --lpg=...
  - Optional --date=YYYY-MM-DD flag (defaults to today)
  - Upserts to fuel_prices table on (fuel_type, effective_date)
  - DGO website scraping stub ready for future implementation

- [x] Session 8: Public API, types, stores, hooks, components, and home page
  - Fixed Supabase Database types (was empty placeholder → full typed schema)
    → Resolved Vercel deploy failure (TS error on rpc() call)
    → Fixed RPC param mismatch: user_lat/user_lng → lat/lng, max_results → result_limit
    → NearbyStation type aligned with actual RPC response (latitude/longitude/distance_meters)
  - API route: GET /api/stations with query params (lat, lng, radius, fuel, brand, q, limit)
    → Validates params, calls nearby_stations RPC, client-side text search filter
    → Cache-Control: public, s-maxage=60
    → Tests: 7 tests (happy path, validation, filters, search, error handling)
  - Vitest config: vitest.config.ts with jsdom, @/ alias, react plugin
  - Zustand stores (3):
    → use-map-store: center, zoom, selectedStationId, flyTo
    → use-filter-store: fuelTypes, radius, searchQuery, brand, toggleFuelType, resetFilters
    → use-preferences-store: locale, preferredFuel, currency, theme, onboarding (persisted to localStorage)
  - Hooks:
    → use-geolocation: browser geolocation with BEIRUT_CENTER fallback, high accuracy, 10s timeout
    → use-stations: fetches /api/stations, debounced search (300ms), abort controller cleanup
  - Components:
    → fuel-badge: colored badge per fuel type (blue/purple/amber/green)
    → station-card: brand dot, locale-aware name/address, distance, fuel badges, 24h tag, selected state
    → station-list: skeleton loading (5 cards), empty state, scrollable list wired to map store
    → station-map: vanilla mapbox-gl, GeoJSON circle layer with brand colors, click/hover handlers, flyTo on selection
    → fuel-type-filter: All + fuel type pill buttons with colored dots, amber active state
    → radius-filter: 1/3/5/10/25 km pill chips
    → search-input: search icon, translated placeholder, clear button, logical properties for RTL
    → filter-bar: composes search + fuel types + separator + radius
    → bottom-nav: fixed mobile nav (Map/List/Prices/Settings), amber active, safe area padding
  - Updated header: added fuel prices + settings buttons, settings hidden on mobile
  - shadcn components added: badge, skeleton, toggle-group, toggle
  - Home page: full split-view layout
    → Desktop: 420px sidebar (filters + station list) + map
    → Mobile: full-screen map + bottom sheet (drag handle, expandable 40%→70%)
    → Wired: geolocation → filter store → useStations → map + list
  - formatDistance() utility: meters → "350m" or "1.2 km"
  - mapbox-gl CSS imported in root layout

- [x] Session 9: Station detail page, API endpoint, and OG image generation
  - API route: GET /api/stations/[id] by UUID or slug
    → Parses PostGIS geography → lat/lng floats
    → Fetches latest fuel prices, deduplicates per fuel_type
    → Cache-Control: public, s-maxage=300
    → Returns { station, prices } or 404
  - Station detail page: src/app/[locale]/station/[id]/page.tsx (SSR)
    → Direct Supabase queries (no self-fetch), generateMetadata with OG image
    → Locale-aware name/address (Arabic if locale=ar and name_ar exists)
    → Brand color accent bar, open/closed status, 24h indicator
    → Fuel badges, address card, phone link, operating hours, amenities badges
    → Fuel prices grid with LBP + USD conversion (LBP_USD_RATE = 89,500)
    → "View on Map" link back to home with lat/lng/zoom params
  - not-found.tsx: translated 404 page with MapPin icon and back-to-map button
  - navigate-button.tsx: client component, opens Google Maps directions
  - share-button.tsx: client component, Web Share API with clipboard fallback + "Copied!" state
  - Dynamic OG image: GET /api/og?slug= (Edge runtime, next/og ImageResponse)
    → 1200x630 PNG with dark background, brand-color accent bar
    → Benzina logo, brand name, station name, address with MapPin icon
    → Fuel type badge pills (blue/purple/amber/green), "Open 24h" badge
    → Cache-Control: public, s-maxage=86400 (24h CDN cache)
  - New i18n keys (en + ar): operatingHours, copied, notFound, notFoundDescription, backToMap, currentPrices, viewOnMap

- [x] Session 10: Fuel prices feature (API + page + ticker)
  - Fixed src/types/price.ts: aligned FuelPrice interface with database.ts fuel_prices.Row
    → Removed stale fields (station_id, price_usd, reported_at)
    → Added price_unit, effective_date, source
  - API route: GET /api/prices
    → Queries fuel_prices, deduplicates to latest per fuel_type
    → Returns { prices: FuelPrice[], lastUpdated: string }
    → Cache-Control: public, s-maxage=3600
  - Prices page: src/app/[locale]/prices/page.tsx (SSR)
    → Direct Supabase query, generateMetadata with SEO description
    → 2x2 color-coded price cards (blue=95, purple=98, amber=diesel, green=lpg)
    → LBP price (large, comma-formatted) + USD conversion
    → Effective date, "Source: DGO" note, weekly update disclaimer
  - Price ticker: src/components/prices/price-ticker.tsx (client component)
    → Fetches /api/prices on mount, dark bar (bg-slate-900, text-amber-400)
    → CSS @keyframes ticker animation with duplicated content for seamless loop
    → Compact 32px height, shows "fuel_name: price LBP" separated by dots
  - Layout: added PriceTicker above Header in [locale]/layout.tsx
  - CSS: added .animate-ticker keyframes to globals.css
  - All translations reused from existing prices.* and fuel.* keys (no new keys needed)

- [x] Session 10: Search improvements (text matching + autocomplete)
  - API route: enhanced /api/stations text search when q param provided
    → Matches across 5 fields: name_en, name_ar, brand, address_en, address_ar
    → Arabic query detection via Unicode range [\u0600-\u06FF]
    → Relevance scoring: exact brand (100) → brand starts-with (90) → name match (65-80) → brand contains (50) → address match (25-30)
    → Arabic queries prioritize name_ar and address_ar in relevance ranking
  - Search input: autocomplete dropdown with suggestions
    → Independent fetch to /api/stations?q=...&radius=200&limit=5 (country-wide)
    → 300ms debounce with abort controller for cancellation
    → Up to 5 suggestions with station name, brand, and address
    → Keyboard navigation (Arrow Up/Down, Enter to select, Escape to close)
    → Selecting navigates to /station/[slug] via i18n useRouter
    → Locale-aware: shows name_ar/address_ar when locale is ar
    → ARIA combobox/listbox attributes for accessibility
    → Closes on blur (only when focus leaves container) and Escape

- [x] Session 11: Welcome onboarding modal
  - WelcomeModal client component with multi-step flow
  - Collects preferred fuels, default radius, currency, theme
  - Persists to preferences store (localStorage), sets hasCompletedOnboarding flag
  - Only shows once on first visit

- [x] Session 12: Settings page
  - src/app/[locale]/settings/page.tsx (client component)
  - Language, preferred fuels, default radius, currency (LBP/USD/both), theme (light/dark/system)
  - Reset all preferences option
  - Wired to preferences store with localStorage persistence

- [x] Session 13: Dark mode support
  - ThemeProvider component wrapping app, reads theme from preferences store
  - Applies .dark class to document, CSS variables switch via dark: variant
  - Mapbox map switches between light-v11 and dark-v11 styles
  - All components support dark mode via Tailwind dark: classes

- [x] Session 14: SEO (sitemap, robots, meta tags, favicon)
  - sitemap.xml: dynamic generation from all station slugs
  - robots.txt: allows all crawlers, references sitemap
  - Metadata: template-based titles, OG + Twitter cards
  - Custom SVG favicon

- [x] Session 15: Admin panel — authentication and dashboard
  - API: POST /api/admin/auth — password auth against ADMIN_PASSWORD env var
    → SHA-256 hashed session cookie (httpOnly, secure, sameSite strict, 24h maxAge)
  - src/lib/admin-auth.ts: verifyAdminSession() helper for API routes
  - Admin layout: client component, shows login form if not authenticated, renders children if authenticated
  - Admin dashboard: stats grid (total stations, fuel types, brands, last scrape)
    → Stations by brand count list, latest fuel prices, last scrape log
    → Links to stations and prices management

- [x] Session 16: Admin station management CRUD
  - API: GET /api/admin/stations — paginated list with search (ilike across name/brand/address/city)
    → Parses PostGIS geography → lat/lng, returns total count
  - API: POST /api/admin/stations — create station, converts lat/lng → POINT(), auto-generates slug
  - API: PUT /api/admin/stations/[id] — partial updates, converts lat/lng if provided
  - API: DELETE /api/admin/stations/[id] — soft delete (sets status to 'closed')
  - Stations list page: shadcn Table with columns (name, brand, status, fuel types, verified)
    → Debounced search, pagination (20/page), click row to edit, "Add Station" button
  - Station edit page: full form (name en/ar, brand dropdown, status dropdown, fuel type checkboxes,
    address en/ar, city, caza, phone, is_24h, amenities, lat/lng)
    → Interactive Mapbox map with draggable marker, click-to-set location
    → Save (POST for new, PUT for edit), Delete with confirmation dialog
    → Toast notifications via sonner
  - shadcn components added: table, input, checkbox, sonner (toast)
  - Toaster added to admin layout

- [x] Session 17: Admin fuel price management
  - API: GET /api/admin/prices — all prices ordered by effective_date DESC (limit 80)
  - API: POST /api/admin/prices — add/upsert price entry
    → Validates fuel_type (95/98/diesel/lpg), price_lbp > 0, effective_date required
    → Upserts on (fuel_type, effective_date) unique constraint, source: 'admin'
  - Prices page: form with date picker (default today) + 4 labeled price inputs
    → "Update Prices" submits all non-empty fields in parallel
    → Price history table: Date, 95, 98, Diesel, LPG columns grouped by date (20 rows)
    → Toast notifications on success/error

## Current
- Core map + list + filters experience is functional
- Station detail pages with SSR, OG images, and sharing are complete
- Fuel prices page, ticker, and API are complete
- Search with autocomplete and relevance ranking is complete
- Settings, onboarding, dark mode, and SEO are complete
- Admin panel is functional: auth, dashboard, station CRUD, price management

## Notes
- React Compiler enabled
- /ar works with dir="rtl" ✓
- Header: sticky, Instrument Serif italic logo, EN/ع locale toggle, prices + settings buttons
- shadcn components: button, badge, skeleton, toggle, toggle-group, card, dialog, label, select, separator, table, input, checkbox, sonner
- DGO scraper: 2499 total stations, 297 in Greater Beirut bounding box
- Data files in data/: dgo-stations-raw.json, osm-stations-raw.json, merged-stations.json
- 316 stations total in merged dataset, ready in Supabase
- Supabase Database types now fully typed (stations, fuel_prices, scrape_logs, nearby_stations RPC)
- Header height ~53px (used in h-[calc(100dvh-53px)] for full viewport layout)
- API surface:
  Public: GET /api/stations, GET /api/stations/[id], GET /api/prices, GET /api/og
  Admin: POST /api/admin/auth, GET/POST /api/admin/stations, PUT/DELETE /api/admin/stations/[id], GET/POST /api/admin/prices
- OG images are edge-rendered with Satori (inline styles only, no external icon libs)
- Price ticker renders above header on all pages (client component, fetches /api/prices)

